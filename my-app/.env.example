# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: For client-side access (if using public bucket)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# AI Configuration (for summary generation)
# Option 1: Use GitHub Models (recommended - requires GitHub token)
GITHUB_TOKEN=github_pat_...

# Option 2: Use OpenAI API (fallback if GitHub token not available)
# OPENAI_API_KEY=sk-...

# Note: GitHub token will be used if available, otherwise falls back to OpenAI key